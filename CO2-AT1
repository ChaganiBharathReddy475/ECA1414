#define TIMEOUT 1000

int sensor_ready()
{
    return 0;
}

int main()
{
    int timeout = TIMEOUT;

    while(!sensor_ready())
    {
        timeout--;

        if(timeout == 0)
        {
            break;
        }
    }

    while(1);
		return 0;
}
